% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lulc_aoi.R
\name{lulc_aoi}
\alias{lulc_aoi}
\title{Create Land Use and Land Cover (LULC) maps and statistics}
\usage{
lulc_aoi(
  aoi,
  year,
  title = NULL,
  output_dir = NULL,
  write_raster = FALSE,
  write_csv = FALSE
)
}
\arguments{
\item{aoi}{sf or SpatVector object.}

\item{year}{Integer between 1985 and 2024.}

\item{title}{Optional map title.}

\item{output_dir}{Directory to save outputs (default working directory).}

\item{write_raster}{Logical; write masked raster as GeoTIFF.}

\item{write_csv}{Logical; write class areas (ha) as CSV.}
}
\value{
Invisibly list with masked SpatRaster and area data frame. If no
        write flags are TRUE, the area data frame is returned visibly.
}
\description{
Downloads MapBiomas Collection 10 raster for a single year, crops to the AOI,
and generates a standardized map. Optionally saves the masked
raster (GeoTIFF) and a CSV of class areas (hectares).
}
\examples{
\dontrun{
library(geobr)
library(terra)

# Get an area of interest (e.g., Colina municipality, SP)
muni <- read_municipality()
aoi <- subset(muni, name_muni == "Colina")
aoi <- vect(aoi)

# Create a map for 2020
lulc_aoi(aoi, year = 2020)

# Save raster and CSV to a specific directory
lulc_aoi(aoi, year = 2020, output_dir = "./results", write_raster = TRUE, write_csv = TRUE)
}

}
